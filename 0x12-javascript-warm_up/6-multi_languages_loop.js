#!/usr/bin/node
// prints in 3 lines: by using an array of string and a loop

const langs = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(langs[i]);
}
